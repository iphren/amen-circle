import type { Dictionary } from "@/lib/i18n/dictionaries/en-GB";
import type { DeepPartial } from "@/lib/i18n/types";

// 简体中文 translations. Fill in keys mirroring the structure of en-GB.ts;
// anything left out (or left blank) automatically falls back to English, so the
// app stays fully functional while this is translated incrementally.
//
// Example:
//   export const zhCN: DeepPartial<Dictionary> = {
//     common: { appName: "Amen Circle", signIn: "登录" },
//   };
export const zhCN: DeepPartial<Dictionary> = {};
